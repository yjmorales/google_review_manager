<nav class="main-header navbar navbar-expand-md navbar-light navbar-white">
    <div class="container">
        <a href="{{ path('index') }}" class="navbar-brand">
            <img src="{{ asset('/img/logo.png') }}" alt="{{ title }}" class="brand-image" style="opacity: .8">
            <span class="brand-text font-weight-light">{{ title }}</span>
        </a>
        <button class="navbar-toggler order-1" type="button" data-toggle="collapse" data-target="#navbarCollapse"
                aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse order-3" id="navbarCollapse">
            <!-- Left navbar links -->
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a id="dropdownSubMenuBusiness"
                       href="#"
                       data-toggle="dropdown"
                       aria-haspopup="true"
                       aria-expanded="false"
                       class="nav-link dropdown-toggle">Business</a>
                    <ul aria-labelledby="dropdownSubMenuBusiness"
                        class="dropdown-menu border-0 shadow"
                        style="left: 0; right: inherit;">
                        <li><a href="{{ path('business_list') }}" class="dropdown-item">Business list</a></li>
                        <li><a href="{{ path('business_create') }}" class="dropdown-item">Create business</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a id="dropdownSubMenuUsers"
                       href="#"
                       data-toggle="dropdown"
                       aria-haspopup="true"
                       aria-expanded="false"
                       class="nav-link dropdown-toggle">Users</a>
                    <ul aria-labelledby="dropdownSubMenuUsers"
                        class="dropdown-menu border-0 shadow"
                        style="left: 0; right: inherit;">
                        <li><a href="{{ path('users_list') }}" class="dropdown-item">User list</a></li>
                        <li><a href="{{ path('user_create') }}" class="dropdown-item">Create user</a></li>
                    </ul>
                </li>
            </ul>
            <!-- SEARCH FORM -->
            <form class="form-inline ml-0 ml-md-3">
                <div class="input-group input-group-sm">
                    <input class="form-control form-control-navbar" type="search" placeholder="Search"
                           aria-label="Search">
                    <div class="input-group-append">
                        <button class="btn btn-navbar" type="submit">
                            <i class="fas fa-search"></i>
                        </button>
                    </div>
                </div>
            </form>
        </div>

        <ul class="order-1 order-md-3 navbar-nav navbar-no-expand ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                <a class="nav-link" data-toggle="dropdown" href="#">
                    <span class="pl-2 pr-2">{{ app.user.username }}</span>
                    <i class="fas fa-user"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                    <a href="{{ path('app_logout') }}" class="dropdown-item">
                        <div class="media">
                            <div class="media-body">
                                <p class="text-sm">Logout</p>
                            </div>
                        </div>
                    </a>
                </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>